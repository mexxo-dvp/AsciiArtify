# Використання kubectl kubeplugin (bash)

Плагін виводить статистику CPU/Memory у CSV-форматі для `pods` або `nodes`.

## Встановлення
```bash
chmod +x scripts/kubeplugin
mkdir -p ~/.local/bin
cp scripts/kubeplugin ~/.local/bin/kubectl-kubeplugin
```